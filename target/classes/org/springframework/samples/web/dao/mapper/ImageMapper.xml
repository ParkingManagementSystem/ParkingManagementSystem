<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.springframework.samples.web.dao.mapper.ImageMapper">

   <insert id="saveImage" parameterType="java.util.Map">
<!--        UPDATE share_parking SET image = RAWTOHEX ('${param1}') where share_parking_code = '${param2}' -->
       UPDATE share_parking SET image = '${param1}' where share_parking_code = '${param2}'
   </insert>
   
</mapper>